# GeoLife Preprocessing Pipeline

## Purpose

`scripts/preprocess_geolife.py` converts extracted GeoLife `.plt` files into a reproducible tabular dataset for WEGOTCHU's Phase 1 route-deviation experiment.

## Run

From the repository root:

```powershell
python scripts\preprocess_geolife.py