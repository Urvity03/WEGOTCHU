# Module: Operational & Utility Scripts (`scripts/`)

## Purpose & Scope
Contains automation scripts for development environment setup, benchmark dataset downloading, model export pipelines, and synthetic telemetry generation.

## Standards
* All scripts must accept standard CLI arguments (`argparse`).
* Scripts must include `--help` flags and clear error messages.
* Never hardcode absolute paths or credentials.
